extends ../layout

include ../includes/_paginate

block content
  h1.main-title 
    | 이벤트를 찾아보세요!  
  .container
    form.form-inline.search-input-group(action='/questions', method='GET')
      i.fa.fa-search
      input.form-control.mr-sm-2(type='text', name='term', placeholder='Search', aria-label='Search', value=term)

    h1.main-title 
      if questions.total > 0
        .small 총 #{questions.total} 개의 이벤트

    if questions.total <= 0 
      .alert.alert-info 아직 이벤트가 없습니다.
    else
      .questions
        each question in questions.docs
          .question
            .row
              | 이벤트 : 
              a.title(href=`/questions/${question.id}`)= question.title
            .row
              .col-md-5
                | 조직 : 
                a.organizeName= question.organizeName
                |  - 
                a.organizeExp= question.organizeExp
              .col-md-7
                | 기간 :
                a.startedAt= question.startedAt
                |  ~ 
                a.finishedAt= question.finishedAt
            .row
              .col-md-5
                | 종류 :
                a.eventSort=question.eventSort
              .col-md-7
                | 분야 :
                a.eventTopic=question.eventTopic
            
            .row
              .col-md-2
                |

            include _question

      +paginate(questions, '/questions', query)

    hr
    a.btn.btn-primary(href='/questions/new') 이벤트 생성하기
