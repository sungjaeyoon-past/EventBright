.btn.btn-outline-success
    | 상세정보
.alert.alert-success
  .row
    .col-md-5
      | 조직 : 
      a.organizeName= question.organizeName
      |  - 
      a.organizeExp= question.organizeExp
    .col-md-7
      | 기간 :
      a.startedAt= question.startedAt
      |  ~ 
      a.finishedAt= question.finishedAt
  .row
    .col-md-5
      | 종류 :
      a.eventSort=question.eventSort
    .col-md-7
      | 분야 :
      a.eventTopic=question.eventTopic

  .row
    .col-md-5
      | 최대 인원 : 
      a.maxPeople=question.maxPeople
    .col-md-7
      | 가격 :
      a.ticket=question.ticket
  .row
    .classlatlng(style="display:none;")
      .col-md-5
        | 위도
        a#latform=question.lat
      .col-md-5
        | 경도
        a#lngform=question.lng

.tags.badges

each tag in question.tags
  span.badge.badge-phill.badge-secondary= tag
  | 
.row
  .btn.btn-outline-primary(style="margin-left:1em;")
    | 부가정보
.col-md-1
.alert.alert-primary
  .col-md-12
    .numbers.
      좋아요 : 
      #[span.number.num-votes= question.numLikes] 
      질문 :
      #[span.number.num-answers= question.numAnswers]
      후기 :
      #[span.number.num-answers= question.numAnswers]
      조회수 :
      #[span.number.num-views= question.numReads]
      신청인원 :
      #[span.number.num-participate= question.numParticipate]
    .author
      | asked on #{moment(question.createdAt).format('YYYY-MM-DD')}
      if question.author
        | 
        | by #[a(href="")= question.author.name] 
